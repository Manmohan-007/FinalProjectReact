const initialState = {}export const UserDetails = (state = initialState, action) => {    switch(action.type) {        case 'UPDATE_USER': return {            ...state,            username: action.data        }        default: return {            ...state        }    }}