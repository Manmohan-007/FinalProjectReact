const initialState = {}export const ActiveModule = (state = initialState, action) => {    switch (action.type) {         case 'ACTIVE_MODULE': return {             ...state,             module: action.data         }        case 'ACTIVE_SUB_MODULE': return {            ...state,            subModule: action.data        }        case 'UPDATE_ACTIVE_DATA': return {            ...state,            data: action.data        }        case 'DAILY_DATA': return {            ...state,            dailyPlan: action.data        }        case 'SESSION_PLAN': return {            ...state,            sessionPlan: action.data        }        case 'MODULE_WEEKS': return {            ...state,            moduleWeeks: action.data        }         default: return {             ...state         }    }}