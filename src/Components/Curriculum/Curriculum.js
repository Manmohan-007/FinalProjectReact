import React from "react";import classes from './Curriculum.module.css'class Curriculum extends React.Component {    state = {        active: 'units'    }    updateActiveSection = (section) => {        this.setState({active: section})        this.props.history.push(section)    }    render() {        return(            <div className={classes.MainContainer}>                <div className={classes.SectionDirectorsContainer}>                    <span onClick={() => this.updateActiveSection('units')} className={`${classes.SectionMenus} ${this.state.active === 'units' ? classes.Active : null}`}>Units</span>                    <span onClick={() => this.updateActiveSection('grades')} className={`${classes.SectionMenus} ${this.state.active === 'grades' ? classes.Active : null}`}>Grades</span>                </div>            </div>        )    }}export default Curriculum