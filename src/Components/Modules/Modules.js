import React from "react";import classes from './Modules.module.css'import { Dropdown } from "../../Assests/Icons";import { Link } from "react-router-dom";import { connect } from 'react-redux'class Modules extends React.Component{    state = {        active: false    }    updateActiveView = (object) => {        this.setState({active: !this.state.active})    }    render() {        return(            <div className={classes.MainContainer}>                <div onClick={(e) => {                    this.updateActiveView(e)                }} className={classes.MainTitleContainer}>                    <div className={classes.ModuleTitle}>Day - {this.props.day} | {this.props.title}</div>                    <div className={`${classes.DropdownContainer} ${this.state.active ? classes.ActiveDrop : null}`}>{ Dropdown }</div>                </div>                <div className={`${classes.ModuleContents} ${this.state.active ? classes.Active : null}`}>                    <Link to={"/classroom/module/session_plan"}><p className={classes.MoreAboutModules}>Session Plan</p></Link>                    <Link to={`/classroom/module/video/${this.props.sessionPlan.recording}`}><p className={classes.MoreAboutModules}>Session Recording</p></Link>                </div>            </div>        )    }}const fetchDataFromGlobalStore = (globalStore) => {    return {        ...globalStore.ModuleReducer    }}export default connect(fetchDataFromGlobalStore)(Modules)