import React from "react";import classes from './UnitsPage.module.css'import { connect } from "react-redux";import ModulePlan from "../../Components/ModulePlan/ModulePlan";import UnitsModule from "../../Components/UnitsModule/UnitsModule";import Modules from "../../Components/Modules/Modules";const UnitsPage = (props) => {    return (        <div className={classes.MainContainer}>            <div className={classes.LeftSection}>                <div className={classes.ModulesSection}>                    <UnitsModule />                </div>                <div className={classes.ModulePlansSection}>                    <ModulePlan />                </div>            </div>            <div className={classes.RightSection}>                {                    props.dailyPlan.map((item) => {                        return <Modules {...item} />                    })                }            </div>        </div>    )}const fetchDataFromGlobalStore = (globalStore) => {    return {        ...globalStore.ModuleReducer,    }}const updateSessionPlanDetails = (dispatch) => {    return {        updateSessionPlanDetails: (data) => dispatch({type: 'SESSION_PLAN', data: data})    }}export default connect(fetchDataFromGlobalStore, updateSessionPlanDetails)(UnitsPage)